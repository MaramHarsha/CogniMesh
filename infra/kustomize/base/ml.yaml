apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ml-control-state
  labels:
    app.kubernetes.io/name: cognimesh
    app.kubernetes.io/component: ml-control
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ml-control
  labels:
    app.kubernetes.io/name: cognimesh
    app.kubernetes.io/component: ml-control
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cognimesh
      app.kubernetes.io/component: ml-control
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cognimesh
        app.kubernetes.io/component: ml-control
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
      containers:
        - name: ml-control
          image: cognimesh/ml-control:0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8100
          env:
            - name: COGNIMESH_ENV
              value: kubernetes
            - name: COGNIMESH_SERVICE_NAME
              value: cognimesh-ml-control
            - name: COGNIMESH_ALLOW_DEV_AUTH
              value: "false"
            - name: COGNIMESH_ML_STATE_PATH
              value: /var/lib/cognimesh/ml-control/ml.db
            - name: COGNIMESH_OBJECT_REGISTRY_URL
              value: http://object-registry:8000
            - name: COGNIMESH_QUERY_SERVICE_URL
              value: http://query-service:8060
            - name: COGNIMESH_MLFLOW_TRACKING_URI
              value: http://mlflow:5000
            - name: COGNIMESH_MLFLOW_ENABLED
              value: "true"
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: ml-state
              mountPath: /var/lib/cognimesh/ml-control
      volumes:
        - name: ml-state
          persistentVolumeClaim:
            claimName: ml-control-state
---
apiVersion: v1
kind: Service
metadata:
  name: ml-control
  labels:
    app.kubernetes.io/name: cognimesh
    app.kubernetes.io/component: ml-control
spec:
  selector:
    app.kubernetes.io/name: cognimesh
    app.kubernetes.io/component: ml-control
  ports:
    - name: http
      port: 8100
      targetPort: http
